module.exports = {
	//ModManager: require('./mod-manager'), // need to rewrite this later
	//Dispatch: require('./connection/dispatch'),
	Connection: require('./connection'),
	FakeClient: require('./clients/FakeClient'),
	RealClient: require('./clients/RealClient')
}